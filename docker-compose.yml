version: "3.9"

services:
  trend-scraper:
    build: .
    container_name: trend-scraper
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./app:/app/app   # Hot-reload in dev (remove in prod)
